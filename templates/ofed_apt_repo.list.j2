#
# Mellanox OFED APT repository configuration for {{ ansible_distribution }} {{ ansible_distribution_version }}
#
# This template constructs the repository entry using role variables so that
# administrators can point to a local mirror or override the base URL.
# The ``signed-by`` option references the Mellanox GPG key stored in the
# keyrings directory.  Do not edit this file directly on the target
# system; instead override the variables ``ofed_repository_url``,
# ``ofed_version`` or ``ofed_repo_dist_name`` in your playbook or
# inventory.

deb [signed-by={{ ofed_apt_key_dest }}] {{ ofed_repository_url }}/{{ ofed_version }}/{{ ofed_repo_dist_name }}/$(ARCH) ./